# Normalise to LF in the repository. Without this, committing from Windows records
# CRLF and the diffs are unreadable to anyone on Linux or macOS.
* text=auto eol=lf

*.png binary
*.npz binary
*.pdf binary
